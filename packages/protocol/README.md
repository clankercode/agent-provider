# @agent-provider/protocol

Versioned wire messages, safe-value encoding, guards, and protocol negotiation
for Agent Provider browser bridges.

```bash
npm install @agent-provider/protocol
```

This package is ESM-only and intended for browser-capable TypeScript or
JavaScript applications. See the Agent Provider repository for the complete
security model and extension setup.
