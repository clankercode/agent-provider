# Consumer output

Implemented headless and React consumption, an extension-backed reference chat,
optional WebMCP exposure, and an operations dashboard with read/write/destructive
tools, page context, form redaction, and extension-owned consent. Extension
popup, approval, options, and dashboard surfaces follow the selected Quiet
Control Room visual direction.

Evidence: example build passes; deterministic actual-browser screenshots cover
desktop/mobile and light/dark states; the screenshot script verifies the real
Chrome extension session handshake.
