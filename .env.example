# Authorized gateway endpoint for live provider smoke tests
# (scripts/test-extension-browser.mjs --live and
# apps/extension/lib/provider-adapters.live.test.ts). Keep the real value out
# of git — .env is gitignored.
AGENT_PROVIDER_LIVE_GATEWAY=
